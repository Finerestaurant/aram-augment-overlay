# Deliberately lean: this gets downloaded on a PC-bang machine.
# OCR is the Windows engine (Windows.Media.Ocr), reached through the split
# winrt namespace packages -- about 3 MB of wheels and no model download at all.
# It replaced rapidocr-onnxruntime + onnxruntime (~64 MB plus a 3.2 MB model),
# which read 4/5 of the captures with known answers and missed the silver
# 믿음직한 무기 outright; the Windows engine reads 5/5 exactly, at 6 ms per crop
# against 87 ms. See docs/FINDINGS.md section 12.
#
# This needs the Korean OCR language pack, which ships with Korean Windows:
#   Add-WindowsCapability -Online -Name "Language.OCR~~~ko-KR~0.0.1.0"
obsws-python>=1.8.0
winrt-Windows.Media.Ocr>=3.2.1
winrt-Windows.Globalization>=3.2.1
winrt-Windows.Graphics.Imaging>=3.2.1
winrt-Windows.Storage.Streams>=3.2.1
winrt-Windows.Foundation>=3.2.1
winrt-Windows.Foundation.Collections>=3.2.1
opencv-python-headless>=4.10
numpy>=2.0
Pillow>=10.0
requests>=2.31
# Tray icon for the status window. Tkinter itself ships with Python; this is the
# only piece of the GUI that needs a download, and the window still runs (window
# close then quits instead of hiding) if it is missing.
pystray>=0.19
