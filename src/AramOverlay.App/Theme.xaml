<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">

    <!-- One dark palette, three depths: the window ground, a surface a step
         above it for cards and bars, and a sunken well below it for the list.
         One saturated accent. Everything else is spacing and weight. -->
    <Color x:Key="BgColor">#17171C</Color>
    <SolidColorBrush x:Key="Bg" Color="#17171C" />
    <SolidColorBrush x:Key="Rail" Color="#17171C" />
    <SolidColorBrush x:Key="Panel" Color="#1E1E25" />
    <SolidColorBrush x:Key="PanelHover" Color="#26262F" />
    <SolidColorBrush x:Key="Sunken" Color="#141419" />
    <SolidColorBrush x:Key="FieldBg" Color="#26262F" />
    <SolidColorBrush x:Key="Row" Color="#2B2B35" />
    <SolidColorBrush x:Key="Line" Color="#2C2C36" />
    <SolidColorBrush x:Key="Text" Color="#E9E9EF" />
    <SolidColorBrush x:Key="Dim" Color="#8B8B98" />
    <SolidColorBrush x:Key="Faint" Color="#5D5D6B" />
    <SolidColorBrush x:Key="Accent" Color="#7F6DF2" />
    <SolidColorBrush x:Key="Ok" Color="#4EC98A" />
    <SolidColorBrush x:Key="Bad" Color="#E0574C" />
    <SolidColorBrush x:Key="Warn" Color="#E8C07A" />
    <!-- The status glyph sits on a 16% wash of its own colour, so the state
         reads from the icon and the words even where the colour does not. -->
    <SolidColorBrush x:Key="OkSoft" Color="#294EC98A" />
    <SolidColorBrush x:Key="BadSoft" Color="#29E0574C" />
    <SolidColorBrush x:Key="WarnSoft" Color="#29E8C07A" />
    <SolidColorBrush x:Key="DimSoft" Color="#298B8B98" />
    <SolidColorBrush x:Key="Silver" Color="#9AA0AC" />
    <SolidColorBrush x:Key="Gold" Color="#E0A83C" />
    <SolidColorBrush x:Key="Prism" Color="#C98FD6" />
    <!-- 16% of each rarity, the ground behind a pick's glyph. -->
    <SolidColorBrush x:Key="SilverSoft" Color="#299AA0AC" />
    <SolidColorBrush x:Key="GoldSoft" Color="#29E0A83C" />
    <SolidColorBrush x:Key="PrismSoft" Color="#29C98FD6" />
    <SolidColorBrush x:Key="Veil" Color="#B3121217" />

    <!-- The widget's own palette, measured off capture frames rather than the
         window's. The two are deliberately different: the window is a dark
         Windows app, the widget is the game's own card art. Previews of the
         widget have to be drawn in the widget's colours or they preview
         something that does not exist. -->
    <SolidColorBrush x:Key="WSilver" Color="#98A4AE" />
    <SolidColorBrush x:Key="WGold" Color="#E3CDA7" />
    <LinearGradientBrush x:Key="WPrism" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F7D774" Offset="0" />
        <GradientStop Color="#8BE9CF" Offset="0.38" />
        <GradientStop Color="#C99AF5" Offset="0.66" />
        <GradientStop Color="#F7A8D8" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="WCardBg" Color="#D6080C14" />
    <SolidColorBrush x:Key="WTrayBg" Color="#C7080D11" />
    <SolidColorBrush x:Key="WStripBg" Color="#B8080D11" />
    <SolidColorBrush x:Key="WInk" Color="#F2F5F8" />
    <SolidColorBrush x:Key="WGlyphBg" Color="#0B1216" />

    <!-- A theme is a look, so it is chosen by looking. Each tile draws the
         widget at roughly a third scale in the widget's own colours; the name
         under it is a label, not an explanation. -->
    <Style x:Key="ThemeTile" TargetType="RadioButton">
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Shell" Background="{StaticResource Sunken}"
                            BorderBrush="{StaticResource Line}" BorderThickness="1"
                            CornerRadius="6" Padding="7" SnapsToDevicePixels="True">
                        <StackPanel>
                            <Border Background="#0A0E13" CornerRadius="4" Height="88" Padding="7">
                                <ContentPresenter VerticalAlignment="Center"
                                                  HorizontalAlignment="Center" />
                            </Border>
                            <TextBlock x:Name="Label" Text="{TemplateBinding Tag}"
                                       Margin="0,7,0,0" FontSize="12"
                                       Foreground="{StaticResource Dim}"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Shell" Property="BorderBrush"
                                    Value="{StaticResource Faint}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Shell" Property="BorderBrush"
                                    Value="{StaticResource Accent}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Shell" Property="BorderBrush"
                                    Value="{StaticResource Accent}" />
                            <Setter TargetName="Shell" Property="Background"
                                    Value="{StaticResource PanelHover}" />
                            <Setter TargetName="Label" Property="Foreground"
                                    Value="{StaticResource Text}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <LinearGradientBrush x:Key="AccentFill" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#7F6DF2" Offset="0" />
        <GradientStop Color="#9B7BF5" Offset="1" />
    </LinearGradientBrush>

    <FontFamily x:Key="UiFont">Segoe UI, Malgun Gothic, Yu Gothic UI, Microsoft YaHei UI</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Mono, Consolas</FontFamily>
    <FontFamily x:Key="GlyphFont">Segoe MDL2 Assets</FontFamily>

    <!-- Thin overlay scrollbar; the stock one dates the whole window. -->
    <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#3A3A47" CornerRadius="4" Margin="2,0" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" />
                            </Track.IncreaseRepeatButton>
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" />
                            </Track.DecreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ScrollContentPresenter />
                        <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1"
                                   Style="{StaticResource ThinScrollBar}"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Title bar buttons: no chrome until hovered, close goes red. -->
    <Style x:Key="CaptionButton" TargetType="Button">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Foreground" Value="{StaticResource Dim}" />
        <Setter Property="FontFamily" Value="{StaticResource GlyphFont}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource PanelHover}" />
                            <Setter Property="Foreground" Value="{StaticResource Text}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="#E03B4B" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Two views, so a segment switcher rather than a rail: the selected one
         carries a 2px accent underline, the other is muted text. -->
    <Style x:Key="SegmentButton" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource Dim}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="14,7,14,9" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid Background="Transparent">
                        <ContentPresenter Margin="{TemplateBinding Padding}" />
                        <Border x:Name="line" Height="2" VerticalAlignment="Bottom"
                                Background="{StaticResource Accent}" Opacity="0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource Text}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="line" Property="Opacity" Value="1" />
                            <Setter Property="Foreground" Value="{StaticResource Text}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Buttons. Ghost is the default: outlined, quiet. Accent is the one
         action on a screen that should draw the eye. -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="13,6" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="Transparent" CornerRadius="5"
                            BorderBrush="{StaticResource Line}" BorderThickness="1">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource FieldBg}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" Background="{StaticResource Accent}" CornerRadius="5">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Field" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource FieldBg}" />
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="CaretBrush" Value="{StaticResource Accent}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Accent}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Width" Value="196" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="5"
                            BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="8,0">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Numbers sit right-aligned in a narrow box, so a column of them lines up. -->
    <Style x:Key="NumField" TargetType="TextBox" BasedOn="{StaticResource Field}">
        <Setter Property="Width" Value="76" />
        <Setter Property="TextAlignment" Value="Right" />
    </Style>

    <Style x:Key="Combo" TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Width" Value="196" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="toggle" Focusable="False" ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                  RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bd" Background="{StaticResource FieldBg}" CornerRadius="5"
                                            BorderBrush="{StaticResource Line}" BorderThickness="1">
                                        <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0"
                                              Stroke="{StaticResource Dim}" StrokeThickness="1.4" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="BorderBrush"
                                                    Value="{StaticResource Accent}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Margin="9,0,28,0" VerticalAlignment="Center"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                               AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                            <Border Background="{StaticResource Panel}" CornerRadius="6" Margin="0,4,0,0"
                                    BorderBrush="{StaticResource Line}" BorderThickness="1"
                                    MinWidth="{TemplateBinding ActualWidth}" MaxHeight="260">
                                <ScrollViewer>
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="{StaticResource Text}" />
                    <Setter Property="Padding" Value="10,7" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="bd" Background="Transparent" CornerRadius="4" Margin="4,1">
                                    <ContentPresenter Margin="{TemplateBinding Padding}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="bd" Property="Background"
                                                Value="{StaticResource PanelHover}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Switch" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="track" Width="38" Height="21" CornerRadius="11"
                                Background="{StaticResource FieldBg}" BorderBrush="{StaticResource Line}"
                                BorderThickness="1">
                            <Ellipse x:Name="knob" Width="15" Height="15"
                                     HorizontalAlignment="Left" Margin="3,0,0,0"
                                     Fill="{StaticResource Dim}" />
                        </Border>
                        <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="track" Property="Background"
                                    Value="{StaticResource Accent}" />
                            <Setter TargetName="track" Property="BorderBrush"
                                    Value="{StaticResource Accent}" />
                            <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="knob" Property="Margin" Value="0,0,3,0" />
                            <Setter TargetName="knob" Property="Fill" Value="White" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="track" Property="BorderBrush"
                                    Value="{StaticResource Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tooltips in the window's own colours; the stock one arrives white. -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8"
                            BorderBrush="{StaticResource Line}" BorderThickness="1" Margin="0,0,6,6">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.45" />
                        </Border.Effect>
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- A small "?" that carries a picture of where to click. -->
    <Style x:Key="HelpDot" TargetType="Border">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Background" Value="{StaticResource DimSoft}" />
        <Setter Property="Cursor" Value="Help" />
        <Setter Property="ToolTipService.InitialShowDelay" Value="150" />
        <Setter Property="ToolTipService.ShowDuration" Value="60000" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Row}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Text roles. Group titles are sentence case: capitals are a web habit. -->
    <Style x:Key="GroupTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="2,0,0,8" />
    </Style>

    <Style x:Key="RowTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Text}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="RowDesc" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Dim}" />
        <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,2,0,0" />
    </Style>

    <Style x:Key="Hint" TargetType="TextBlock" BasedOn="{StaticResource RowDesc}">
        <Setter Property="Foreground" Value="{StaticResource Faint}" />
    </Style>

    <!-- A thin bar that only says "still running". Windows reports no
         percentage for a Feature-on-Demand install, so nothing pretends to be
         one: a 30% segment slides end to end. The slide is done by animating the
         scale origin, which needs no pixel widths and so no code-behind. -->
    <Style x:Key="ActivityBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="3" />
        <Setter Property="Background" Value="{StaticResource Line}" />
        <Setter Property="Foreground" Value="{StaticResource Accent}" />
        <Setter Property="IsIndeterminate" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}" CornerRadius="1.5" ClipToBounds="True">
                        <!-- The storyboard hangs off the segment itself, not the
                             template: a bar that starts out Collapsed raises Loaded
                             before its template exists, and a template-level
                             trigger then fails to find the segment by name. -->
                        <Border Background="{TemplateBinding Foreground}" CornerRadius="1.5"
                                RenderTransformOrigin="0,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="0.3" />
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <PointAnimation Storyboard.TargetProperty="RenderTransformOrigin"
                                                            From="0,0.5" To="1,0.5" Duration="0:0:1.3"
                                                            AutoReverse="True">
                                                <PointAnimation.EasingFunction>
                                                    <SineEase EasingMode="EaseInOut" />
                                                </PointAnimation.EasingFunction>
                                            </PointAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The settings card: one bordered surface, rows divided by a hairline.
         The first row carries no top border; every row after it does. -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Panel}" />
        <Setter Property="BorderBrush" Value="{StaticResource Line}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="7" />
        <Setter Property="Margin" Value="0,0,0,18" />
    </Style>

    <Style x:Key="CardRow" TargetType="Border">
        <Setter Property="Padding" Value="14,12" />
        <Setter Property="BorderBrush" Value="{StaticResource Line}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
    </Style>

    <Style x:Key="CardRowFirst" TargetType="Border" BasedOn="{StaticResource CardRow}">
        <Setter Property="BorderThickness" Value="0" />
    </Style>

</ResourceDictionary>
